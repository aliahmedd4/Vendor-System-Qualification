# Dependencies
node_modules/

# Secrets — never commit real credentials (use .env.example as the template)
.env
.env.*
!.env.example

# TLS material generated by generate-certs.sh / .ps1
docker/certs/

# Generated OQ evidence + Playwright artefacts (regenerated per execution run)
evidence/
test-results/
playwright-report/
blob-report/
.playwright/

# Editor / OS noise
.DS_Store
Thumbs.db
*.log
